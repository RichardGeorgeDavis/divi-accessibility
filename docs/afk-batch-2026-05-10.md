# AFK Batch Run: 2026-05-10

## Batch 1: Current Status And Stale Information

- Release target: `2.1.7`
- Current published maintained-fork release before this batch: `2.1.6`
- `2.1.6` GitHub Release asset status: packaged zip present; `.zip.sha256` asset absent.
- Upstream PR `#121`: closed as of the 2026-05-10 status check.
- Upstream issue `#122`: open as of the 2026-05-10 status check.
- WordPress.org `accessible-divi` listing: closed since 2020-05-12.
- WordPress current release from the core version-check API: `6.9.4`.

## Batch 2: Stale-Info Policy

- Treat `README.md`, `readme.txt`, `divi-accessibility.php`, `package.json`, `package-lock.json`, and `languages/divi-accessibility.pot` as version-bearing release metadata that must agree before release.
- Treat older `docs/release-*`, `docs/afk-batch-*`, and upstream handover docs as historical evidence unless their heading explicitly says "current".
- Keep current external claims dated or tied to a release batch. Re-check GitHub, WordPress.org, and WordPress core APIs before publishing release notes.
- Do not update runtime-tested claims from package/static checks alone. Use `docs/testing/manual-accessibility-checklist.md` and record exact WordPress, Divi, PHP, browser, and third-party plugin versions.

## Batch 3: Cleanup Decisions

- Remove ignored `.DS_Store` files only.
- Leave the ignored `ref/` tree in place as local-only audit/reference material. It is not tracked and is not included in packaged releases.
- Leave unmerged cached worktree branches untouched during this release batch. Revisit them after `2.1.7` is published and CI has settled.

## Batch 4: Implementation Scope

- Harden GitHub release updates by requiring both exact `divi-accessibility-X.X.X.zip` and `divi-accessibility-X.X.X.zip.sha256` assets.
- Verify update package downloads against the published SHA-256 checksum before WordPress installs them.
- Generate `.sha256` files during packaging and enforce checksum asset expectations in release checks.
- Preserve existing settings while merging new defaults during activation and runtime loading.
- Fix packaged translation loading.
- Keep the current frontend/admin reliability fixes for inline style injection, hash-link focus, search focus handlers, mobile menu keyboard activation, and Divi 5 builder metadata access.

## Batch 5: Verification Log

- `npm run build`: pass
- `npm run i18n`: pass with the known `node-wp-i18n` dynamic-property deprecation under the local PHP runtime.
- `DA11Y_RELEASE_VERSION=2.1.7 DA11Y_ASSUME_YES=true npm run release`: pass
- `npm run lint`: pass
- `npm run release:check`: pass
- `git ls-files '*.php' | xargs -n 1 php -l`: pass
- `npm audit --audit-level=moderate`: pass, 0 vulnerabilities
- Package output: `packaged/divi-accessibility-2.1.7.zip`
- Package checksum output: `packaged/divi-accessibility-2.1.7.zip.sha256`
- Package SHA-256: `6819326003004c5210956b3e475bbd1e27940b8c8c7f5a0416c8c9458aa9fada`
- Checksum verification from `packaged/`: pass
- Zip contents: expected plugin payload only under `divi-accessibility/`, including `admin/`, `includes/`, `languages/`, `public/`, `divi-accessibility.php`, `readme.txt`, and `uninstall.php`.
- LocalWP runtime smoke:
  - `blueprint` WordPress files are present and report WordPress `6.9.4`, but WP-CLI could not reach the configured database host.
  - `master-licenses` WordPress files are present and report WordPress `6.9.4`, but WP-CLI could not establish a database connection.
  - No activation/runtime claims were updated from these blocked LocalWP checks.
