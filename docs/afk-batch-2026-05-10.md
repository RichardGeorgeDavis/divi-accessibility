# AFK Batch Run: 2026-05-10

## Batch 1: Current Status And Stale Information

- Original release target for this batch: `2.1.7`
- Current maintained-fork release after closeout: `2.1.7`
- Published release URL: <https://github.com/RichardGeorgeDavis/divi-accessibility/releases/tag/2.1.7>
- Previous published maintained-fork release before this batch: `2.1.6`
- `2.1.6` GitHub Release asset status: packaged zip present; `.zip.sha256` asset absent.
- `2.1.7` GitHub Release asset status: packaged zip present; `.zip.sha256` asset present.
- Upstream PR `#121`: closed as of the 2026-05-10 status check.
- Upstream issue `#122`: open as of the 2026-05-10 status check.
- WordPress.org `accessible-divi` listing: closed since 2020-05-12.
- WordPress current release from the core version-check API: `6.9.4`.

## Batch 2: Stale-Info Policy

- Treat `README.md`, `readme.txt`, `divi-accessibility.php`, `package.json`, `package-lock.json`, and `languages/divi-accessibility.pot` as version-bearing release metadata that must agree before release.
- Treat older `docs/release-*`, `docs/afk-batch-*`, and upstream handover docs as historical evidence unless their heading explicitly says "current".
- Keep current external claims dated or tied to a release batch. Re-check GitHub, WordPress.org, and WordPress core APIs before publishing release notes.
- Do not update runtime-tested claims from package/static checks alone. Use `docs/testing/manual-accessibility-checklist.md` and record exact WordPress, Divi, PHP, browser, and third-party plugin versions.

## Batch 3: Cleanup Decisions

- Remove ignored `.DS_Store` files only.
- Leave the ignored `ref/` tree in place as local-only audit/reference material. It is not tracked and is not included in packaged releases.
- Leave unmerged cached worktree branches untouched during this release batch. Revisit them after `2.1.7` is published and CI has settled.

## Batch 4: Implementation Scope

- Harden GitHub release updates by requiring both exact `divi-accessibility-X.X.X.zip` and `divi-accessibility-X.X.X.zip.sha256` assets.
- Verify update package downloads against the published SHA-256 checksum before WordPress installs them.
- Generate `.sha256` files during packaging and enforce checksum asset expectations in release checks.
- Preserve existing settings while merging new defaults during activation and runtime loading.
- Fix packaged translation loading.
- Keep the current frontend/admin reliability fixes for inline style injection, hash-link focus, search focus handlers, mobile menu keyboard activation, and Divi 5 builder metadata access.

## Batch 5: Verification Log

- `npm run build`: pass
- `npm run i18n`: pass with the known `node-wp-i18n` dynamic-property deprecation under the local PHP runtime.
- `DA11Y_RELEASE_VERSION=2.1.7 DA11Y_ASSUME_YES=true npm run release`: pass
- `npm run lint`: pass
- `npm run release:check`: pass
- `git ls-files '*.php' | xargs -n 1 php -l`: pass
- `npm audit --audit-level=moderate`: pass, 0 vulnerabilities
- Package output: `packaged/divi-accessibility-2.1.7.zip`
- Package checksum output: `packaged/divi-accessibility-2.1.7.zip.sha256`
- Package SHA-256: `6819326003004c5210956b3e475bbd1e27940b8c8c7f5a0416c8c9458aa9fada`
- Checksum verification from `packaged/`: pass
- Zip contents: expected plugin payload only under `divi-accessibility/`, including `admin/`, `includes/`, `languages/`, `public/`, `divi-accessibility.php`, `readme.txt`, and `uninstall.php`.
- LocalWP runtime smoke:
  - `blueprint` WordPress files are present and report WordPress `6.9.4`, but WP-CLI could not reach the configured database host.
  - `master-licenses` WordPress files are present and report WordPress `6.9.4`, but WP-CLI could not establish a database connection.
  - No activation/runtime claims were updated from these blocked LocalWP checks.

## Batch 6: Post-Release Closeout

- Release commit: `d2bddd36a5511bf14c20e3d24b38ae3f0e6193ba`
- Tag: `2.1.7`
- GitHub Actions status for the release commit: pass, run <https://github.com/RichardGeorgeDavis/divi-accessibility/actions/runs/25629974111>
- GitHub Release API asset check: pass for `divi-accessibility-2.1.7.zip` and `divi-accessibility-2.1.7.zip.sha256`.
- Downloaded release checksum verification: pass from a fresh download into `/tmp/divi-a11y-2.1.7-verify`.
- Verified release package SHA-256: `6819326003004c5210956b3e475bbd1e27940b8c8c7f5a0416c8c9458aa9fada`.
- Runtime-tested claims were held until the packaged `2.1.7` zip passed the local Divi 4 and Divi 5 checks recorded below.

## Batch 7: Runtime Environment Recovery

- LocalWP services were not initially reachable through the app-managed runtime.
- `blueprint` database service was recovered manually from the Local site metadata:
  - Site id: `olL4csA2S`
  - MySQL port: `10028`
  - Socket: `~/Library/Application Support/Local/run/olL4csA2S/mysql/mysqld.sock`
- `master-licenses` database service was recovered manually from the Local site metadata:
  - Site id: `DtXj3cx7m`
  - MySQL port: `10053`
  - Socket: `~/Library/Application Support/Local/run/DtXj3cx7m/mysql/mysqld.sock`
- `tester` was used as a supplemental Divi 5 matrix site because it had reachable Divi 5 module content:
  - Site id: `bOFdGT-Iv`
  - MySQL port: `10055`
  - Socket: `~/Library/Application Support/Local/run/bOFdGT-Iv/mysql/mysqld.sock`
- Temporary PHP built-in servers were used only for local smoke checks:
  - `blueprint`: `http://127.0.0.1:18089/` with an HTTPS/host router for `blueprint.local`
  - `master-licenses`: `http://127.0.0.1:18088/`
  - `tester`: `http://127.0.0.1:18090/` with an HTTPS/host router for `tester.local`
- No LocalWP database credentials or tracked project files were changed.

## Batch 8: Packaged Plugin Smoke

- `blueprint` packaged-plugin smoke passed:
  - WordPress `6.9.4`
  - Divi `4.27.6`
  - PHP `8.5.3`
  - Baseline plugin state: Divi Accessibility `2.1.1`, inactive
  - Installed `packaged/divi-accessibility-2.1.7.zip` with `wp plugin install --force --activate`.
  - Deactivate/reactivate passed.
  - Existing option values were preserved and new defaults were merged.
  - Frontend smoke confirmed `_da11y.version` `2.1.7`, `da11y-divi-4`, skip-link focus to `#main-content`, and mobile menu role/name/state.
- `master-licenses` packaged-plugin smoke passed:
  - WordPress `6.9.4`
  - Divi `5.4.1`
  - PHP `8.5.3`
  - Baseline plugin state: Divi Accessibility `2.1.4`, active
  - Installed `packaged/divi-accessibility-2.1.7.zip` with `wp plugin install --force --activate`.
  - Explicit deactivate/reactivate passed and triggered the activation-time option merge.
  - Existing option values were preserved and new defaults were merged.
  - Frontend smoke confirmed `_da11y.version` `2.1.7`, `da11y-divi-5`, skip-link focus to `#main-content`, mobile menu role/name/state, search close naming, and cart naming.
- `tester` supplemental Divi 5 packaged smoke passed:
  - WordPress `6.9.4`
  - Divi `5.3.3`
  - PHP `8.5.3`
  - Baseline plugin state: Divi Accessibility `2.1.4`, active
  - Installed `packaged/divi-accessibility-2.1.7.zip`, then deactivated/reactivated successfully.
  - Existing option values, including `reduced_motion`, were preserved while new defaults were merged.
- No `divi-accessibility` PHP warnings or fatals were recorded during smoke testing. Existing local logs showed WP-CLI/PHP deprecations and Divi-theme warnings unrelated to this plugin; the early Divi contact-form warning came from an invalid local fixture and stopped after the fixture was corrected.

## Batch 9: Manual Accessibility Matrix

- Local test pages were created only on local test sites:
  - `blueprint`: `Codex 2.1.7 Manual Matrix`, page id `25109374`, slug `codex-2-1-7-manual-matrix`
  - `tester`: `Codex 2.1.7 Manual Matrix`, page id `397557`, slug `codex-2-1-7-manual-matrix`
- Browser/runtime evidence:
  - Browser: Chrome through Playwright MCP, 1366 by 900 viewport
  - Plugin package: Divi Accessibility `2.1.7`
  - WordPress: `6.9.4`
  - PHP: `8.5.3`
  - Divi 4 target: `blueprint`, Divi `4.27.6`
  - Divi 5 matrix target: `tester`, Divi `5.3.3`
  - Divi 5 packaged-plugin smoke target: `master-licenses`, Divi `5.4.1`
- Divi 4 matrix passed for the checked plugin-owned behavior:
  - `_da11y.version` `2.1.7`
  - `da11y-divi-4` body class
  - skip link moved focus to `#main-content` and added `tabindex="-1"`
  - mobile menu had role `button`, accessible name `Menu`, `aria-expanded="false"`, and `tabindex="0"`
  - search module had `role="search"` plus plugin-added screen-reader label for the generated input
  - slider previous/next labels and dot labels were present and focusable
  - contact text field exposed label, `aria-required="true"`, and `aria-invalid="false"`
  - contact checkbox labels exposed `role="checkbox"`, `tabindex="0"`, and Space toggled the checkbox plus `aria-checked`
  - tabs exposed tablist/tab/tabpanel roles and associations
  - accordion/toggle titles exposed button semantics, focusability, and expanded state
  - image alt/title helpers and social-link accessible names were present
- Divi 5 matrix passed for the checked plugin-owned behavior:
  - `_da11y.version` `2.1.7`
  - `da11y-divi-5` body class
  - skip link moved focus to `#main-content` and added `tabindex="-1"`
  - mobile menu had role `button`, accessible name `Menu`, `aria-expanded="false"`, and `tabindex="0"`
  - search form had `aria-label="Search form"`, plugin-added screen-reader input label, named close-search control, and a screen-reader submit button
  - cart link exposed accessible name `View cart`
  - slider previous/next labels and dot labels were present and focusable
  - contact text field exposed label, `aria-required="true"`, and `aria-invalid="false"`
  - contact checkbox labels exposed `role="checkbox"`, `tabindex="0"`, and Space toggled the checkbox plus `aria-checked`
  - tabs exposed tablist/tab/tabpanel roles and associations
  - accordion/toggle titles exposed button semantics, focusability, and expanded state
  - image alt/title helpers and social-link accessible names were present
  - D4-style shortcode content rendered and was enhanced on a Divi 5 site
- Browser console reported one site/Divi script error under the temporary routed-server setup. No matching plugin PHP error or `divi-accessibility` script failure was observed, so no `2.1.8` code work was opened from it.
- This matrix is compatibility evidence for plugin-owned behavior. It is not a full WCAG conformance claim.

## Batch 10: Issue Triage

- Fork issues remain disabled, so upstream issues are still the public backlog reference.
- Upstream issues `#122`, `#96`, `#91`, `#90`, `#88`, `#81`, `#73`, `#72`, `#69`, `#60`, and `#52` were rechecked on 2026-05-10 and remain open.
- `docs/upstream-triage-map.md` now separates the current post-`2.1.7` triage queue from historical `2.1.0` closeout notes.
- No upstream comments were posted during this AFK batch.
- No code work was opened because the packaged `2.1.7` runtime checks did not reproduce a plugin defect.

## Batch 11: Focused 2.1.8 Prep Decision

- No `2.1.8` code branch was cut.
- The next batch should stay documentation/backlog-focused unless a reporter or local runtime test reproduces a defect against the packaged `2.1.7` zip.
- Candidate cleanup after release:
  - prune or archive stale cached worktrees only after confirming they are not needed for active validation
  - leave unmerged local branches untouched until each branch has an explicit disposition
  - continue using dated external-status checks for GitHub, WordPress.org, and WordPress core claims

## Batch 12: Post-Closeout Verification

- `npm run package`: pass
- `unzip -t packaged/divi-accessibility-2.1.7.zip`: pass
- `shasum -a 256 -c divi-accessibility-2.1.7.zip.sha256` from `packaged/`: pass
- Fresh local package SHA-256 after repackaging: `1a3df890bbc95c188b577aec8aff2e37c9d558bed4c211633259ad1f25526f84`
- Zip contents after repackaging: expected plugin payload only under `divi-accessibility/`.
- `npm run lint`: pass
- `npm run release:check`: pass
- `git ls-files '*.php' | xargs -n 1 php -l`: pass
- `npm audit --audit-level=moderate`: pass, 0 vulnerabilities
- The published release asset SHA-256 remains `6819326003004c5210956b3e475bbd1e27940b8c8c7f5a0416c8c9458aa9fada`; the fresh local package SHA above is only the regenerated local archive checksum.

## Batch 13: CI Stale-Action Cleanup

- Post-closeout GitHub Actions run `25634531790` passed for commit `e531ed03214f0d0e7558ed16bfef3dab888fba9d`.
- That run emitted GitHub's Node.js 20 action-runtime deprecation warning for `actions/checkout@v4` and `actions/setup-node@v4`.
- Updated `.github/workflows/ci.yml` to `actions/checkout@v5` and `actions/setup-node@v6`.
- Kept the project test runtime pinned to Node `20`; this cleanup updates the GitHub Action runtime, not the plugin's Node compatibility target.
